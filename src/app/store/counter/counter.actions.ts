import {createAction} from '@ngrx/store';

export const incrementButtonPressed = createAction('[Counter] Increment button pressed');
export const decrementButtonPressed = createAction('[Counter] Decrement button pressed');

